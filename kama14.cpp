//自己写的狗屎代码 

#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    string s;
    string res;
    cin>>n;
    //此处需要吸收一个回车
    getchar();
    while(n--){
        bool flag = false; //false表示不能往res里面放东西
        getline(cin,s);
        //s[0]先做特殊处理
        if(s[0]<=90 && s[0]>=65){
            res.push_back(s[0]);
        }else if(s[1]<=122 && s[1]>=97){
            res.push_back(s[0]-32);
        }

        for(int i=1;i<s.size()-1;i++){
            if(s[i] == ' ' && (s[i+1]<=90 || s[i+1]>=65 || s[i+1]<=122 || s[i+1]>=97 )){
                if(s[0]<=90 && s[0]>=65){
                    res.push_back(s[0]);
                }else if(s[1]<=122 && s[1]>=97){
                    res.push_back(s[0]-32);
                }

            }
        }
        cout<<res<<endl;
    }


    return 0;
}


//istringstream的用法
string s = "1 2 3 4 5";
istringstream iss(s);

int x;
while (iss >> x) {
    cout << x << " ";
}


#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    string s;
    
    string k;
    cin>>n;
    //此处需要吸收一个回车
    getchar();
    while(n--){
        getline(cin,s);
        //对每一行数据进行流处理
        istringstream str(s);
        //会默认按“空白字符”分割，而且这一点非常重要、非常好用。
        string res;
        // 能够获取分割后的每一段字符串
        while(str >> k){
            //cout<<k<<endl;
            if(k[0]>='a' && k[0]<='z'){
                k[0] = k[0]-32;
            }
            res = res + k[0];
        }
        cout<<res<<endl;
    }



    return 0;
}

是的 ✅ **会默认按“空白字符”分割**，而且这一点非常重要、非常好用。

我给你说清楚，而且结合你刷题时最容易踩的坑来讲。

---

## 一、结论先行（一句话）

> **`istringstream >>` 默认以“空白字符”作为分隔符**

这里的“空白字符”包括：

* 空格 `' '`
* 制表符 `\t`
* 换行符 `\n`

而且：
👉 **连续多个空白，会被当成一个分隔符**

---

## 二、最直观的例子

```cpp
string s = "A   B\tC    D";
istringstream iss(s);

string x;
while (iss >> x) {
    cout << x << endl;
}
```

输出：

```
A
B
C
D
```

✔ 多个空格 / tab 都没问题
✔ 完全不用你自己处理空格

---

## 三、对比你之前的写法（为什么你会出 bug）

### ❌ 手动 `i += 2`（非常危险）

```cpp
for (int i = 0; i < s.size(); i += 2) {
    char c = s[i];
}
```

如果输入是：

```
A  B   C
```

👉 直接错位，甚至读到空格

---

### ✅ `istringstream` 的正确方式

```cpp
istringstream iss(s);
char c;
while (iss >> c) {
    // 每次读一个有效字符
}
```

你**完全不关心空格有几个**

---

## 四、它为什么“默认按空格分割”？（原理一点点）

`operator>>` 的规则是：

1. **先跳过所有前导空白**
2. 从第一个非空白字符开始读取
3. 读到下一个空白为止

所以：

```cpp
iss >> x;
```

等价于：

> “帮我读下一个被空白隔开的 token”

---

## 五、常见数据类型下的表现

### 1️⃣ 读 `int`

```cpp
string s = "  10   20  30 ";
istringstream iss(s);

int x;
while (iss >> x) cout << x << endl;
```

✔ 正常

---

### 2️⃣ 读 `string`

```cpp
string s = "hello   world  cpp";
istringstream iss(s);

string w;
while (iss >> w) cout << w << endl;
```

✔ 正常

---

### 3️⃣ 读 `char`

```cpp
string s = "A B   C";
istringstream iss(s);

char c;
while (iss >> c) cout << c << endl;
```

输出：

```
A
B
C
```

⚠️ 注意：
`>> char` **会跳过空白**
如果你想读“空格本身”，那就不能用 `>>`（要用 `get()`）

---

## 六、如果我**不想**按空格分割怎么办？

那就不用 `>>`，改用：

### 用 `getline(iss, token, 分隔符)`

```cpp
string s = "A,B,C,D";
istringstream iss(s);

string token;
while (getline(iss, token, ',')) {
    cout << token << endl;
}
```

---

## 七、回到你刷的题（平均绩点那题）

你之前最正确的写法应该是：

```cpp
string line;
getline(cin, line);

istringstream iss(line);
char grade;

bool ok = true;
int sum = 0, cnt = 0;

while (iss >> grade) {
    if (data.find(grade) == data.end()) {
        ok = false;
        break;
    }
    sum += data[grade];
    cnt++;
}
```

✔ 自动按空格拆
✔ 不怕多个空格
✔ 不用自己数下标

---

## 八、一句话送你（很重要）🧠

> **只要你看到“一行里有多个数据，用空格隔开”——第一反应就是 `getline + istringstream`**

---

如果你愿意，我可以下一步：

* 帮你总结一个 **OJ 输入处理万能模板**
* 或拿你之前那道 **“平均绩点”**，帮你写一版 100% 标准答案
* 或专门讲 **`stringstream / istringstream / ostringstream` 区别**

你挑一个 👌
